import { Suspense } from "react";
import { generateOpenGraph } from "@/utils";
import OAuth from "./OAuth";

export const metadata = generateOpenGraph({
  title: "rq89u3rgbifhcv90ui-9jopiknj",
  description:
    "?엥엥ㅇ/엥?ㅔ엥엥?ㅔㅇ엥ㅇ엥?ㅇ엥? rq89u3rgbifhcv90ui-9jopiknj ithin the first hour of trading, establishing a new record high. However, the precious metal's momentum soon re입니다.",
});

const Page = () => {
  return (
    <Suspense>
      <OAuth />
    </Suspense>
  );
};

export default Page;
